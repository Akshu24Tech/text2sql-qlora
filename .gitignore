# local notes, not part of the submission
re-fresh-*.md
notes/
docs/recording-script.md

# python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
.env

# model weights / adapters are too big for git (regenerate by running notebook 2)
outputs/
*.safetensors
*.bin

# but keep the small result artifacts (they're the evidence for the report):
!results/metrics.json
!results/predictions.json
